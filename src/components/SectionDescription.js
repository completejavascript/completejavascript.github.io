import React from 'react';

const SectionDescription = ({ content }) => {
  return (
    <p className="text-description text-center margin-bottom-3">{content}</p>
  )
}

export default SectionDescription;
